<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.ai.project.infrastructure.ProjectMemberMapper">
    <select id="selectActiveMember" resultType="org.dromara.ai.project.domain.ProjectMember">
        select id, project_id, user_id, role, status, create_dept, create_by, create_time,
               update_by, update_time, del_flag
        from ai_project_member
        where project_id = #{projectId} and user_id = #{userId} and status = 'ACTIVE' and del_flag = 0
        limit 1
    </select>
</mapper>
