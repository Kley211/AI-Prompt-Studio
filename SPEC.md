# AI Prompt Studio 项目规格说明

版本：v0.1

状态：Draft

最后更新：2026-08-21

## 1. 项目定义

AI Prompt Studio 是一个面向企业团队的 AI Prompt、模型和低代码工作流治理平台。用户在项目和 RBAC 权限控制下配置模型、编写 Prompt、创建不可变版本、在线调试、审核发布，并通过 API 将已发布的 Prompt 或工作流提供给外部业务系统调用。

平台不负责模型训练，而是负责将现有大模型能力转化为可管理、可测试、可发布、可追踪的企业 AI 能力。

## 2. 产品边界

### 2.1 MVP 必须包含

- 用户登录、系统角色、菜单和接口权限。
- 项目、项目成员和项目级数据权限。
- 模型供应商、模型和加密凭证配置。
- Prompt 模板、变量、输入输出 Schema。
- Prompt 不可变版本、版本比较、发布和回滚。
- 在线调试、流式输出、Token、耗时和费用统计。
- API Key 和已发布 Prompt 的 OpenAPI 调用。
- 工作流版本、基础 DAG 校验和最小节点执行。
- 工作流运行记录和节点运行详情。
- 基础操作审计日志。

### 2.2 MVP 明确不包含

- 模型训练、微调和 GPU 集群管理。
- 完整 RAG/知识库、复杂 Agent、多 Agent 协作。
- 多人实时协同编辑。
- 插件市场、在线支付和商业计费。
- Kubernetes、多集群和微服务治理。
- 任意代码执行节点。
- 通用 OA、CRM、ERP 或 BPM 平台能力。

## 3. 角色与权限

系统角色由 RBAC 管理，项目角色由项目成员关系管理。

| 角色 | 权限边界 |
|---|---|
| 系统管理员 | 管理系统用户、角色、模型供应商和全局配置 |
| 项目所有者 | 管理项目、成员和项目全部资源 |
| 项目管理员 | 管理项目成员和项目资源 |
| 开发者 | 创建、编辑、调试 Prompt 和工作流 |
| 发布者 | 审核、发布、回滚版本 |
| 观察者 | 查看资源和运行结果 |
| API 调用者 | 只能调用已发布资源 |

验收要求：任何业务接口都必须同时校验系统功能权限和项目数据权限；不能仅依靠前端隐藏按钮实现授权。

## 4. 模块规格

### M0 基础工程

任务：初始化模块化单体、统一异常、统一响应、日志、配置、数据库迁移、OpenAPI 文档、Docker Compose 开发环境。

边界：不修改 RBAC 框架核心实现；业务代码放在独立模块中。

验收标准：后端、前端、PostgreSQL、Redis 可一键启动；健康检查可用；数据库迁移可重复执行；未登录接口返回 401，越权接口返回 403。

### M1 项目空间与数据权限

任务：项目 CRUD、项目成员、项目角色、成员邀请/移除、项目资源隔离。

边界：第一版单组织部署；不实现 SaaS 计费和跨租户管理。

验收标准：项目 A 成员无法读取项目 B 资源；Viewer 不能编辑；项目管理员不能操作其他项目；权限检查覆盖直接 API 调用。

### M2 模型供应商与模型网关

任务：供应商配置、OpenAI Compatible 模型、凭证加密、连接测试、流式/非流式调用、超时、重试、Token 和费用计算。

边界：第一版不实现模型训练、不托管模型文件；业务模块不得直接调用供应商 SDK。

验收标准：至少接入一个 OpenAI Compatible 模型；API Key 不以明文返回或写入日志；调用结果包含模型、Token、耗时和错误信息。

### M3 Prompt 管理

任务：Prompt CRUD、System/User 模板、变量解析、输入输出 Schema、标签、版本比较、草稿/测试/发布/归档状态。

边界：第一版只支持普通变量替换，不支持任意模板代码和脚本执行。

验收标准：模板变量自动解析；缺少必填变量时拒绝调用；已发布版本不可修改；修改发布内容必须创建新版本；支持回滚到历史版本。

### M4 在线调试与运行记录

任务：变量输入、模型参数、流式响应、停止生成、运行记录、脱敏策略、Token/费用/耗时展示。

边界：第一版不实现复杂评测集和自动评分。

验收标准：用户可以调试草稿或指定版本；每次调用可追踪；失败调用保存错误信息；日志支持 FULL、MASKED、METADATA_ONLY 三种内容模式。

### M5 发布 API

任务：项目 API Key、哈希保存、启停、过期时间、Prompt/工作流调用接口、限流和基础配额。

边界：第一版不实现在线支付和精细化商业计费。

验收标准：外部调用只依赖 API Key、资源编码和输入变量；原始 Key 仅创建时显示一次；未发布版本不可被公开 API 调用；调用有 Trace ID。

### M6 低代码工作流

任务：工作流 CRUD、版本、Vue Flow 画布、节点配置、后端 DAG 校验、运行和节点详情。

MVP 节点：START、END、PROMPT、LLM、CONDITION、HTTP。

边界：第一版不支持任意代码执行、复杂循环、并行调度和人工审批节点。

验收标准：可创建并运行 `START -> LLM -> END`；节点 ID 唯一；必须有 START/END；拒绝非法连线、不可达节点和环路；前端 JSON 不能绕过后端校验；已发布工作流版本不可变。

### M7 审计与可观测性

任务：登录、权限、资源变更、发布、回滚、API 调用和异常审计；运行 Trace、节点状态和基础指标。

边界：第一版不接入完整 ELK、Prometheus 或商业 APM。

验收标准：关键操作可按用户、项目、资源、时间和 Trace ID 查询；敏感凭证和敏感输入不出现在普通日志中。

## 5. 核心业务流程验收

必须完整跑通以下流程：

```text
登录 -> 创建项目 -> 添加成员 -> 配置模型 -> 创建 Prompt
-> 定义变量 -> 在线调试 -> 保存版本 -> 发布
-> API 调用 -> 查看 Token、耗时、费用和审计记录
```

工作流流程：

```text
创建工作流 -> 拖入 START/LLM/END -> 配置节点
-> 后端校验 -> 调试 -> 发布 -> API 调用 -> 查看节点运行详情
```

## 6. 非功能要求

- 安全：凭证加密；API Key 哈希；默认不记录完整敏感输入；所有资源接口服务端鉴权。
- 可维护性：模块边界清晰；Controller 不直接编写模型调用和复杂业务逻辑；数据库变更使用 Flyway。
- 可测试性：核心权限、版本、模板渲染、DAG 校验和模型网关必须有单元测试；关键流程使用 Testcontainers 做集成测试。
- 可部署性：Docker Compose 可启动完整 MVP；配置通过环境变量或配置文件注入，不提交密钥。
- 可观测性：每次模型或工作流执行必须有 executionId 和 traceId。

## 7. 里程碑

| 里程碑 | 交付内容 | 完成标志 |
|---|---|---|
| P0 | 基础工程和 RBAC | 登录、权限、健康检查、迁移可用 |
| P1 | 项目与模型 | 项目隔离，模型可测试调用 |
| P2 | Prompt MVP | Prompt 可创建、调试、发布和回滚 |
| P3 | API 与审计 | 外部可调用，调用可追踪 |
| P4 | 工作流 MVP | START/LLM/END 可拖拽、校验、运行和发布 |
| P5 | 稳定性 | 测试、脱敏、限流、Docker 文档完成 |

## 8. Definition of Done

每个任务只有同时满足以下条件才算完成：

1. 数据库迁移、后端接口和前端入口均已提交。
2. 正常路径、权限失败和参数错误均有处理。
3. 相关单元测试或集成测试通过。
4. API 文档和运行说明已更新。
5. 不引入明文密钥、跨项目数据泄露或不可审计的发布行为。

## 9. 后续阶段

MVP 稳定后再评估：知识库/pgvector、数据集评测、LLM-as-a-Judge、RabbitMQ Worker、Ollama、本地化部署、多租户和审批流。这些能力不属于当前 MVP 的完成条件。
